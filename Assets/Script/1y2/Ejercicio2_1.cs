using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio2_1 : MonoBehaviour
{

    //VARIABLES

    int edad = 3;
    string codigoPostal = ""; //Considero que no es n número con el que vaya a operar por lo que lo consideraría un dato string.
    float altura = 1.75f;
    char genero = 'M';
    string nombre = "Sander";
    int numeroDeHijos = 0;
    float iva = 2f;
    int tallaCamisetas = 42;
    float peso = 75.2f;
    float precio = 22.40f;
    bool alumnoRepetidor = false;
    string mensaje = "Escucha Navigating de Twenty One Pilots";
    char letra = 'B';
    bool mayorDeEdad = true;
    int minutos = 8; //¿Esto contaría como ocho minutos y medio? si no se usaría un int.
    int dias= 5; 
    string matriculaCoche = "1800GVW"; //contiene letras y números, además no se usa para operar.
    int contador = 25;
    char tallaCamiseta = 'L';








    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
