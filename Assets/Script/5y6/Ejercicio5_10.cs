using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class Ejercicio5_10 : MonoBehaviour
{

    //Desarrolla un programa que lea una variable temperatura por el Inspector y  
    //    compruebe en qué tipo de clima se encuentra nuestro personaje en función a la  siguiente tabla.

    // Start is called before the first frame update
   void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
